/// The environment that the app can be running in
enum Environments {
  /// Production environment, used for customers.
  prod,

  /// Staging environment, used for QA
  staging,

  /// Development environment, used for development work on the app
  dev,
}
